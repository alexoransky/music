PyFluidSynth 1.3.0 is used to drive FluidSynth.
If the same version is installed, copy fluidsynth.py from patched directory to
the install directory, for instance /usr/lib/python3.9/site-packages .
Otherwise, patch the fluidsynth.py.